var config = {
    "gameofthrones": {
        "firebase": {
            "service_account": {
                "cred": "needs credentials"
            },
            "database_url": "needs credentials"
        }
    },
};

module.exports = config;